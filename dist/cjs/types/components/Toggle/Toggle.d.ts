import React from "react";
import { ToggleButtonProps } from "./Toggle.types";
declare const Toggle: ({ isActive, children, style, className }: ToggleButtonProps) => React.JSX.Element;
export { Toggle };
