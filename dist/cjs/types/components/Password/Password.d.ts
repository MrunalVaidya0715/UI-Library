import React from "react";
import { PasswordProps } from "./Password.types";
declare const Password: ({ placeholder, isMandtory, onChange, style, className, ...props }: PasswordProps) => React.JSX.Element;
export { Password };
