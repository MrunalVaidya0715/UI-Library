import React from "react";
import { CheckboxProps } from "./Checkbox.types";
declare const Checkbox: ({ label, checked, onChange, style, className, ...props }: CheckboxProps) => React.JSX.Element;
export { Checkbox };
