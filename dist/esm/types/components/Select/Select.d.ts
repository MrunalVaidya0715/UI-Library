import React from "react";
import { SelectProps } from "./Select.types";
declare const Select: ({ options, value, onChange, variant, label, style, className, ...props }: SelectProps) => React.JSX.Element;
export { Select };
